//逆时针阴阳信号发生器（顶层文件）
module baguatu (P_out, Q, clk, en);
	input clk, en;
	output [2:0] P_out;
	output [2:0] Q;
	
	baguatu1 counter (Q, clk, en);    //八进制计数器
	baguatu2 decoder ( P_out, Q);    //译码器
	
endmodule
